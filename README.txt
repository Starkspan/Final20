Bereit für Render:

1. Upload ZIP zu GitHub oder direkt zu Render.
2. Secret File anlegen: google-ocr-key.json (aus deinem JSON-Key).
3. Umgebungsvariable setzen:
   GOOGLE_APPLICATION_CREDENTIALS = /etc/secrets/google-ocr-key.json